import 'package:iconly/iconly.dart';

abstract final class AppIcons {
  static const edit = IconlyLight.edit;
  static const graph = IconlyLight.graph;
  static const arrowDown = IconlyBroken.arrow_down_2;
}
