name: core_widgets
description: "A new Flutter package project."
version: 0.0.1
# homepage:

environment:
  sdk: 3.5.0

dependencies:
  core_utils: ^0.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
