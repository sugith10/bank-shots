import 'package:flutter/material.dart';

final class CompleteGoalPage extends StatelessWidget {
  const CompleteGoalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
